const baseUrl = 'https://jsonplaceholder.typicode.com';

const CONSTANTS = {
    endopints: {
        posts: baseUrl + '/posts',
        post: baseUrl + '/post',
        comments: baseUrl + '/comments',
        users: baseUrl + '/users'
    }
}

export default CONSTANTS;